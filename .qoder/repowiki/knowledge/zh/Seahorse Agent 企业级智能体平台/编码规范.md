- 后端严格遵循 Clean Architecture，领域逻辑位于 `kernel`，技术实现位于 `adapter`，通过 `ports` 接口契约通信。
- 前端采用模块化路由与状态管理（Zustand/Context），API 调用封装于 `services` 层，UI 组件基于 shadcn/ui 体系。
- 所有模块共享统一的 `.env` 配置规范与 Docker 部署描述，确保开发、测试与生产环境的一致性。