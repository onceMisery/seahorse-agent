- 采用 Zustand Store 作为单一事实来源，UI 组件通过自定义 Hook（如 `useAuth`）或直接订阅 Store 获取状态，避免 Props 层层传递。
- API 响应拦截器统一处理业务错误码（code !== '0'），将认证相关错误映射为统一的会话过期处理流程，实现前后端认证状态同步。
- 本地存储操作（localStorage）封装在 `storage` 工具类中，并提供 try-catch 保护以防止隐私模式下的存储异常导致应用崩溃。