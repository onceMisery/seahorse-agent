- 作为单一入口模块，通过 `SeahorseAgentApplication` 定义 `scanBasePackages` 限定在 `com.miracle.ai.seahorse.agent` 命名空间，并排除 `RedissonAutoConfigurationV2` 以避免自动配置冲突。
- 依赖两个内部 starter（`seahorse-agent-spring-boot-starter-core` 和 `seahorse-agent-spring-boot-starter-all`）实现功能模块化装配，自身仅保留启动类与最小化配置。
- 通过 `pom.xml` 中的 `spring-boot-maven-plugin` 配置 `classifier=exec` 生成可执行 JAR，明确指定 `mainClass` 确保启动入口唯一性。
- 集成 PostgreSQL、Redisson、Actuator 及 Prometheus 监控，体现运行时基础设施依赖的外部化装配策略。