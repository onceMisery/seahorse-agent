- 端口接口以 `Port` 结尾（如 `ToolGatewayPort`），入站接口以 `InboundPort` 结尾，体现六边形架构边界。
- 领域对象采用不可变记录类（Record）或包含私有构造器的值对象，确保状态一致性。
- 应用服务类以 `Service` 或 `Engine` 命名，编排器类以 `Pipeline` 或 `Loop` 命名，明确职责分工。
- 空对象模式广泛使用，通过 `noop()` 静态方法提供默认无操作实现，避免空指针检查。