- **分层结构**：划分为 `zh/content`（中文技术百科）、`aegis`（Aegis 治理工作区）和 `company-agent`（企业级规划）三大子域。
- **核心入口**：`docs/README.md` 作为全局索引，`docs/aegis/INDEX.md` 维护治理记录，`docs/aegis/BASELINE-GOVERNANCE.md` 定义架构宪法。
- **内容组织**：`zh/content` 采用模块化目录结构覆盖后端内核、前端系统、数据库设计及 API 接口；`aegis/plans` 存放 SaaS MVP 等实施蓝图；`aegis/work` 按日期归档意图、检查点与证据链。
- **依赖方向**：所有文档均指向代码实现（如 `seahorse-agent-kernel`），通过 `BASELINE-GOVERNANCE.md` 约束设计与实现的漂移检测。