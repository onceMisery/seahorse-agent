# Project Context: Seahorse Agent

## Language

| Term | Definition | Avoid |
|------|-----------|-------|
| 核心 Agent/RAG 运行时 | 直接支撑用户完成认证、知识入库与检索、对话、模型上下文、工具沙箱执行以及会话恢复的最小产品闭环。 | 完整企业平台、全部现有功能、后台功能合集 |
| 生产证据门禁 | 在真实依赖环境中用可重复证据证明核心正常路径、失败路径和恢复路径满足发布条件的准入规则。 | 能启动、单测通过、CI 显示绿色、类已经存在 |
| 非核心隔离区 | 不属于当前生产承诺、默认关闭、不得被核心运行时反向依赖并等待迁移或退役的能力集合。 | 已完成能力、可生产能力、只是隐藏的菜单 |
| 显式功能禁用 | 对不可用能力返回稳定且可诊断的禁用结果，而不是空数据、伪成功或无证据的自动降级。 | 优雅降级、返回空值也算成功、暂时忽略错误 |
| 核心外部契约 | 核心运行时对使用方承诺保持稳定的 HTTP/SSE 行为和可迁移业务数据。 | 内部 Java 接口、Bean 名称、模块目录、全部历史 API |

## Relationships

- **核心 Agent/RAG 运行时**只有通过**生产证据门禁**后才能被称为可生产。
- **非核心隔离区**不得成为**核心 Agent/RAG 运行时**的运行前置条件。
- 被隔离能力应通过**显式功能禁用**保护**核心外部契约**。

## Flagged Ambiguities

- “基础功能” -> 统一为**核心 Agent/RAG 运行时**（2026-07-27）。
- “已实现”或“CI 绿色” -> 不再等同于通过**生产证据门禁**（2026-07-27）。
- “保留兼容” -> 仅指保护**核心外部契约**，不要求冻结内部结构（2026-07-27）。

