# 09-advanced-rag.md 修改说明

> 日期：2026-06-05  
> 修改原因：明确 Rerank 功能为**完全可选**（默认禁用），避免强制依赖外部 API

---

## 📝 主要修改

### 1. 新增"快速决策指南"（文档开头）

**位置**：第 8 行之后

**内容**：
- 对比表：不启用 vs 启用 Rerank（成本/延迟/效果）
- 配置示例：默认不配置任何参数即可
- 降级策略说明

**目的**：让用户在阅读前就知道 Rerank 是可选的，不会产生意外成本。

---

### 2. 新增"3.0 Rerank 可选性设计"章节

**位置**：§3 技术方案开头

**内容**：
- **3.0.1 默认行为**：不配置参数时使用 `RerankModelPort.noop()`（原样返回）
- **3.0.2 启用条件**：必须满足 4 个条件（配置启用 + 指定 Provider + API Key + 知识库勾选）
- **3.0.3 成本与性能对比**：具体数字对比表

**目的**：从技术层面明确 Rerank 的可选性实现机制。

---

*本修改确保 Rerank 功能是**完全可选的**，不会给未启用的用户带来任何成本或复杂度。*
