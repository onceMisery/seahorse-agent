# Memory P3 router context weaver - Checkpoint

- Task ID: 2026-05-20-memory-p3-router-context-weaver
- Current todo: P3 implementation candidate is complete; prepare commit.
- Active slice: verification-and-commit
- Completed todos:
  - P2 committed as `2f95f05 feat(memory): add deterministic ingestion workflow`.
  - Baseline read set reviewed for Router, ContextWeaver, chat prompt, AgentLoop, and Spring wiring.
  - RED tests added for question-aware routing, budgeted Context Weaver zones, and AgentLoop ContextWeaver injection.
  - `DefaultMemoryRouter` now activates tracks by question type instead of always pulling episodic memory.
  - `DefaultContextWeaver` now emits ordered zones and enforces item/character budget.
  - Chat fallback, AgentLoop, and Local RAG prompt construction now consume `ContextWeaverPort`.
  - Spring auto-configuration now wires `ContextWeaverPort` into chat, agent, and local RAG prompt paths.
- Blocked on: none
- Evidence refs:
  - `.\mvnw.cmd -pl seahorse-agent-tests -am "-Dtest=MemoryWorkflowRoutingTests,KernelAgentLoopTests#memoryContextIsInjectedThroughConfiguredContextWeaver" test "-Dspotless.check.skip=true" "-Dsurefire.failIfNoSpecifiedTests=false"` -> 7 tests, BUILD SUCCESS.
  - `.\mvnw.cmd -pl seahorse-agent-tests -am "-Dtest=MemoryWorkflowRoutingTests,DefaultMemoryEnginePortTests,KernelAgentLoopTests,KernelChatPipelineTests,LocalRagPromptAdapterTests,SeahorseAgentKernelAutoConfigurationTests#shouldRegisterMemoryManagementAndGovernancePortsWhenMemoryStoresExist" test "-Dspotless.check.skip=true" "-Dsurefire.failIfNoSpecifiedTests=false"` -> 55 tests, BUILD SUCCESS.
  - `.\mvnw.cmd -pl seahorse-agent-spring-boot-starter -am -DskipTests package "-Dspotless.check.skip=true"` -> BUILD SUCCESS.
  - `git diff --check` -> exit 0, CRLF warnings only.
- DriftCheckDraft:
  - Scope: aligned with P3 read-path plan.
  - Compatibility: existing memory engine, `MemoryPromptFormatter`, `ChatResponsePorts` four-arg constructor, `KernelAgentLoop` existing constructors, and `LocalRagPromptAdapter` no-arg constructor retained.
  - Retirement: direct runtime prompt injection sites moved behind `ContextWeaverPort`; `MemoryPromptFormatter` remains as compatibility facade for legacy tests and callers.
  - Decision: continue to commit.
- Next step: Commit P3 as `feat(memory): add question-aware context weaving`.
