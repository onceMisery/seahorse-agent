{
  "schemaVersion": "aegis.schema.v0",
  "requestedOutcome": "Continue the AI-Infra implementation from the current handoff in an isolated worktree.",
  "goal": "Implement approval query and decision APIs, durable checkpoints, WAITING_APPROVAL pause, and resume from checkpoint while preserving the existing hexagonal architecture.",
  "successEvidence": [
    "Kernel approval, checkpoint, WAITING_APPROVAL, and resume focused tests pass.",
    "JDBC approval and checkpoint repository focused tests pass.",
    "Web approval, resume, and checkpoint endpoint focused tests pass.",
    "Spring auto-configuration focused tests pass."
  ],
  "stopCondition": "Stop when the handoff slice is implemented, focused verification passes, or a blocker requires user input.",
  "nonGoals": [
    "No workflow engine.",
    "No remote agent mesh.",
    "No direct tool execution from approval APIs.",
    "No kernel dependency on Spring, JDBC, or Web adapters."
  ],
  "scope": "Approval management API, checkpoint persistence, WAITING_APPROVAL runtime pause, resume from checkpoint, Web endpoints, JDBC adapters, and Spring wiring.",
  "changeKinds": [
    "kernel ports and services",
    "JDBC adapter",
    "Web adapter",
    "Spring auto-configuration",
    "focused tests"
  ],
  "riskHints": [
    "runtime state transitions",
    "approval decision semantics",
    "checkpoint serialization",
    "cross-module wiring"
  ]
}
